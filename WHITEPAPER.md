# WHITEPAPER: 可执行提示词工程 (EPE)

> **版本：1.0.0 | 日期：2026-05-12**

---

## 摘要

可执行提示词工程（Executable Prompt Engineering, EPE）是一种全新的提示词范式。它将自然语言提示词定义为「源码」，将 AI 定义为「编译器」，将最终输出定义为「可直接运行的可执行产物」。本文详细阐述了 EPE 的核心理念、技术架构、标准格式和工程实践。

---

## 1. 背景与动机

自 LLM 普及以来，提示词工程 (Prompt Engineering) 一直处于「自然语言指令」的阶段。用户写一段描述，AI 回复一段文字——本质上与搜索引擎的「输入→输出」没有区别。

这个模式有三个根本性问题：

1. **不可复现** —— 同一提示词在不同时间执行，产出结果高度依赖模型状态
2. **不可版本化** —— 自然语言段落无法在 Git 中进行差异对比和版本管理
3. **不可工序化** —— 产物是「建议」而非「执行体」，需要人工二次转化

EPE 旨在解决这三个问题。

---

## 2. 核心公式：P→C→E→A

```
Prompt（源码） → Compile（AI编译） → Executable（可执行物） → Action（现实结果）

缩写：P → C → E → A
```

### 2.1 P - Prompt（源码）

EPE 的提示词不是自然语言段落，而是结构化的 PROGRAM 块，包含 7 个必填字段：NAME、VERSION、PURPOSE、INPUT、LOGIC、CONSTRAINT、OUTPUT。

### 2.2 C - Compile（编译）

AI 不再「回答」提示词，而是「编译」提示词。编译过程包括：解析字段、理解约束、生成代码/配置/脚本、验证约束覆盖、产出可执行物。

### 2.3 E - Executable（可执行物）

编译产物必须是可直接运行的文件或指令：Python 脚本、Shell 命令、HTML 文档、YAML 配置、SQL 查询等。不能用文字「建议」替代。

### 2.4 A - Action（动作）

产物被执行后产生的现实结果：文件写入磁盘、报告在浏览器中打开、服务成功部署、消息送达手机。

---

## 3. 三大核心规则

### 规则1：提示词具备「源码属性」

| 属性 | 含义 | 验收标准 |
|------|------|---------|
| 可编译 | AI 能将其解析为确定性的执行计划 | 多次执行结果一致 |
| 可复现 | 同一提示词在不同时间产出本质相同 | 产物差异可控 |
| 可逆向 | 从代码可反推出原始意图 | 约束与代码一一对应 |
| 可扩展 | 修改参数即可调整行为 | 改 INPUT 不改 LOGIC |
| 可维护 | 他人能理解提示词的结构和意图 | 无需额外解释 |

### 规则2：AI = 编译器

AI 的输出不是「建议」而是「编译结果」。错误处理遵循编译器逻辑——报错而不是模糊回答。遇到不满足的约束时，抛出明确的编译错误，指出违规位置。

### 规则3：输出必须「可直接运行」

产物不依赖人工解读或修改。放在目标环境中就能执行。验收标准是客观的：运行成功与否，而非感受好坏。

---

## 4. PROGRAM 标准格式

```
[PROGRAM]
NAME:      <项目名>
VERSION:   <语义化版本号>
PURPOSE:   <一句话执行目标——要达成的结果>
INPUT:     <输入条件：数据源、参数、依赖>
LOGIC:     <核心逻辑：3-7步，→分隔>
CONSTRAINT: <约束条件：至少2条边界规则>
OUTPUT:    <最终可执行物描述>
[END]
```

详见 [SPEC.md](SPEC.md)。

---

## 5. 与传统提示词的对比

实验证明，同一需求用传统提示词需要 **4 轮对话**才能收敛，而 EPE PROGRAM 格式只需 **1 轮**。详见 [examples/](examples/) 中的对比示例。

---

## 6. 适用边界

**适合 EPE：** 代码生成、报告生成、数据处理 ETL、定时自动化、API 集成、测试用例生成、配置文件生成。

**不适合 EPE：** 开放式创意写作、闲聊陪伴、需要人类主观判断的任务、战略决策支持。

---

## 7. 未来方向

- **EPE 编译器 CLI** —— 读入 .epe 文件，调用 LLM API，自动编译并执行
- **EPE CI/CD** —— GitHub Actions 集成，.epe 文件变更自动编译测试
- **EPE 模板市场** —— 社区共享的 PROGRAM 模板库
- **IDE 插件** —— 为 .epe 文件提供语法高亮和自动补全

---

*EPE (Executable Prompt Engineering) 是一个开放的方法论标准。*
*提示词即源码，自然语言即编程语言。*
