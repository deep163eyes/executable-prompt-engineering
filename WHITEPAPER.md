# WHITEPAPER: 可执行提示词工程 (EPE)

> **版本：1.0.0 | 日期：2026-05-12**

---

## 摘要

可执行提示词工程（Executable Prompt Engineering, EPE）是一种全新的提示词范式。它将自然语言提示词定义为「源码」，将 AI 定义为「编译器」，将最终输出定义为「可直接运行的可执行产物」。

---

## 1. 背景

传统提示词有三个根本问题：
1. **不可复现** —— 同一提示词在不同时间产出不同
2. **不可版本化** —— 自然语言无法在 Git 中管理
3. **不可工序化** —— 产物是「建议」而非可执行体

## 2. 核心公式：P→C→E→A

```
Prompt → Compile → Executable → Action
```

### 2.1 P - Prompt（源码）
结构化的 PROGRAM 块，5 个必填字段：NAME、GOAL、STEPS、RULES、OUTPUT。

### 2.2 C - Compile（编译）
AI 不再「回答」提示词，而是「编译」提示词：解析字段、理解规则、生成代码/脚本、验证覆盖、产出可执行物。

### 2.3 E - Executable（可执行物）
必须是可直接运行的文件或指令：Python 脚本、Shell 命令、HTML 文档、YAML 配置。

### 2.4 A - Action（动作）
产物被执行后的现实结果：文件写入、服务部署、消息送达。

## 3. 三大核心规则

### 规则1：提示词具备「源码属性」
可编译 · 可复现 · 可逆向 · 可扩展 · 可维护

### 规则2：AI = 编译器
输出不是「建议」而是「编译结果」。报错而非模糊回答。

### 规则3：输出必须「可直接运行」
验收标准是客观的：运行成功与否，而非感受好坏。

## 4. PROGRAM 标准格式

```
[PROGRAM]
NAME:   <项目名>
GOAL:   <一句话执行目标——要达成的结果>
STEPS:  <核心逻辑：3-7步，→分隔>
RULES:  <约束条件：至少2条边界规则>
OUTPUT: <最终可执行物描述>
[END]
```

详见 [SPEC.md](SPEC.md)。

## 5. 适用边界

**适合：** 代码生成、报告生成、数据处理 ETL、定时自动化、API 集成、测试用例生成
**不适合：** 创意写作、闲聊陪伴、需要主观判断的任务

## 6. 未来方向

- **EPE 编译器 CLI** —— 读入 .epe 文件，调用 LLM API 编译执行
- **EPE CI/CD** —— GitHub Actions 集成，自动编译测试
- **EPE 模板市场** —— 社区共享的 PROGRAM 模板库

---

*EPE (Executable Prompt Engineering) 是一个开放的方法论标准。*
*提示词即源码，自然语言即编程语言。*
