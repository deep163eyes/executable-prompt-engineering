# WHITEPAPER: 可执行提示词工程 (EPE)

> **版本：1.0.0 | 日期：2026-05-12**

---

## 摘要

可执行提示词工程（Executable Prompt Engineering, EPE）是一种全新的提示词范式。它将自然语言提示词定义为「源码」，将 AI 定义为「编译器」，将最终输出定义为「可直接运行的可执行产物」。

---

## 1. 背景

传统提示词三个根本问题：**不可复现、不可版本化、不可工序化**。

## 2. 核心公式：P→C→E→A

```
Prompt → Compile → Executable → Action
```

## 3. PROGRAM 标准格式

```
[PROGRAM]
SKILL:   <项目名>
GOAL:   <一句话目标>
STEPS:  <核心步骤，→ 连接>
RULES:  <约束条件>
OUTPUT: <可执行物描述>
                     ← VERSION 缺省: 1.0.0
                     ← INPUT 缺省: 来自上下文
[END]
```

5 个必填字段 + 2 个可选字段（VERSION / INPUT），不写则自动填充默认值。

## 4. 三大核心规则

**规则1：提示词具备源码属性** — 可编译 · 可复现 · 可逆向 · 可扩展 · 可维护
**规则2：AI = 编译器** — 报错而非模糊回答
**规则3：输出可直接运行** — 客观验收标准

## 5. 适用边界

**适合：** 代码生成、报告生成、数据处理 ETL、定时自动化、API 集成
**不适合：** 创意写作、闲聊陪伴、需要主观判断的任务

## 6. 未来方向

- EPE 编译器 CLI
- EPE CI/CD 集成
- EPE 模板市场

---

*EPE (Executable Prompt Engineering) — 提示词即源码，自然语言即编程语言。*
