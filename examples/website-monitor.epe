[PROGRAM]
NAME: 网站变更监控脚本
GOAL: 生成可直接运行的 Python 网站变更监控脚本
STEPS: HTTP GET → 计算页面哈希 → 对比上次哈希 → 不一致则发邮件
RULES: 仅用标准库、敏感信息从 .env 读取、哈希持久化到本地文件
OUTPUT: monitor.py + .env.template（python3 monitor.py 即可运行）
[END]
