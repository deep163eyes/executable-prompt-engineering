[PROGRAM]
SKILL: 定时任务注册
GOAL: 注册一个定时自动执行的任务并指定交付方式
STEPS: 确定执行逻辑 → 设置调度规则 → 注册cronjob → 验证执行结果
RULES: 任务可独立运行（不依赖会话上下文）、失败后重试3次、指定交付渠道
OUTPUT: cronjob（按调度自动执行并通过指定渠道交付）
INPUT: 调度表达式、任务描述、交付方式
VERSION: 1.0
[END]
